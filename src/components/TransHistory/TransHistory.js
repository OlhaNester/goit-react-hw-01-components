import React from 'react';

//import PropTypes from "prop-types";
import TransList from './TransList';

const TransHistory = ({ items }) => {
  return <TransList items={items} />;
};

export default TransHistory;
